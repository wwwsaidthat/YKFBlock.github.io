# 日期专题--暴力遍历模板

```c++
#include <bits/stdc++.h>
using namespace std;
int main(){
    ios:sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    for(int year = 2022 ; year <= 2024;year++){
        for(int month = 1 ; month <= 12 ; month++){
            for(int day = 1; day <= 31 ; day++){
                if(month == 1 || month = 3 || month == 5 || month == 7 || month == 8 || month == 10 || month ==12) ;
                else if(month == 2){
                    if(year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
                        if(day > 29) break;
                
                    }else {
                        if(day > 28) break;
                    }
                }else {if(day > 30) break;}
            }
        }
    }
}
```



# 例题1：

在日常生活中，通过年、月、日这三个要素可以表示出一个唯一确定的日期。

牛牛习惯用 8 位数字表示一个日期，其中，前 4 位代表年份，接下来 2 位代表月份，最后 2 位代表日期。显然：一个日期只有一种表示方法，而两个不同的日期的表示方法不会相同。

牛牛认为，一个日期是回文的，当且仅当表示这个日期的 8 位数字是回文的。现在，牛牛想知道：在他指定的两个日期之间包含这两个日期本身），有多少个真实存在的日期是回文的。

提示：

一个 8 位数字是回文的，当且仅当对于所有的 i (1≤*i*≤8) 从左向右数的第 i 个数字和第 9 - i 个数字（即从右向左数的第 i*i* 个数字）是相同的。

例如：

1. 对于 2016 年 11 月 19 日，用 8 位数字 20161119 表示，它不是回文的。
2. 对于 2010 年 1 月 2 日，用 8 位数字 20100102 表示，它是回文的。
3. 对于 2010 年 10 月 2 日，用 8 位数字 20101002 表示，它不是回文的。

每一年中都有 12 个月份：

其中，1,3,5,7,8,10,12 月每个月有 31 天；4,6,9,11 月每个月有 30 天；而对于 2 月，闰年时有 29 天，平年时有 28 天。

一个年份是闰年当且仅当它满足下列两种情况其中的一种：

1. 这个年份是 4 的整数倍，但不是 100 的整数倍；
2. 这个年份是 400 的整数倍。

例如：

1. 以下几个年份都是闰年：2000,2012,2016。
2. 以下几个年份是平年：1900,2011,2014。

### 输入描述

输入两行，每行包括一个 8 位数字。

第一行表示牛牛指定的起始日期。

第二行表示牛牛指定的终止日期。

保证 datei 和都是真实存在的日期，且年份部分一定为 4 位数字，且首位数字不为 0。

保证 date1一定不晚于 date2

### 输出描述

输出一个整数，表示在 date1和 date2 之间，有多少个日期是回文的。



1. 输入进来 -- cin string.substr() stoi
2. 遍历日期，设置一个flag 如果到了规定的日期中间，设为true否则仍为false -- 模板
3. 对于flag是true的合成字符串检测是否为回文序列  -- reverse  sprintf

注意 sprintf这个东西必须要用char* 不能是string 而且char数组要开大一点，最后还有一个'\0'  可以更大但是不能小 那就稍微开大点100 1000的 别开将将巴巴



```c++
#include <bits/stdc++.h>
using namespace std;
bool checkDate(string s){
  //8位
  string t = s;
  reverse(s.begin(),s.end());
  return s == t;
}
int main()
{
  string start,end;
  cin >> start >> end;
//  cout << start << end;
  int startYear = stoi(start.substr(0,4)),endYear = stoi(end.substr(0,4));
  int startMonth = stoi(start.substr(4,2)),endMonth = stoi(end.substr(4,2));
  int startDay = stoi(start.substr(6)),endDay = stoi(end.substr(6));
//  cout << startYear << startMonth << startDay;
  bool flag = false;
  char check[9] ={0};

  int ans = 0;
  for(int year = startYear ; year <= endYear ;year++){
    for(int month = 1 ; month <= 12; month++){
      for(int day = 1 ; day <= 31; day++){
        if(month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12);
        else if(month == 2){
          if((year % 4 == 0 && year % 100 != 0) || year % 400 == 0){
            if(day > 29) break;
          }else{
            if(day > 28) break;
          }
        }else{
          if(day > 30) break;
        }

        if(year == startYear && month == startMonth && day == startDay) flag = true;

        if(flag){
          sprintf(check,"%d%02d%02d",year,month,day);
          if(checkDate(check)) ans++;
        }
        if(year == endYear && month == endMonth && day == endDay) {flag = false;break;}
      }
    }
  }
  cout << ans;
  return 0;
}
```

# 例题2

### 题目描述

2020 年春节期间，有一个特殊的日期引起了大家的注意：2020 年 2 月 2 日。因为如果将这个日期按 “yyyymmdd” 的格式写成一个 8 位数是 20200202，恰好是一个回文数。我们称这样的日期是回文日期。

有人表示 20200202 是 “千年一遇” 的特殊日子。对此小明很不认同，因为不到 2 年之后就是下一个回文日期：20211202 即 2021 年 12 月 2 日。

也有人表示 20200202 并不仅仅是一个回文日期，还是一个 ABABBABA 型的回文日期。对此小明也不认同，因为大约 100 年后就能遇到下一个 ABABBABA 型的回文日期：21211212 即 2121 年 12 月 12 日。算不上 “千年一遇”，顶多算 “千年两遇”。

给定一个 8 位数的日期，请你计算该日期之后下一个回文日期和下一个 ABABBABA 型的回文日期各是哪一天。

### 输入描述

输入包含一个八位整数 N*N*，表示日期。

对于所有评测用例，10000101≤N≤8999123110000101≤*N*≤89991231，保证 N*N* 是一个合法日期的 8 位数表示。

### 输出描述

输出两行，每行 1 个八位数。第一行表示下一个回文日期，第二行表示下一个 ABABBABA 型的回文日期。

```c++
#include <bits/stdc++.h>
using namespace std;
bool check1(string s){
    string t = s;
    reverse(s.begin(),s.end());
    return t == s;
}
bool check2(string s){
    return (s[0] == s[2] && s[2] == s[5] && s[5] == s[7])  && (s[1] == s[3] && s[3] == s[4] && s[4] == s[6]);
}
int main()
{
    string date;
    cin >> date;
    int y = stoi(date.substr(0,4));
    int m = stoi(date.substr(4,2));
    int d = stoi(date.substr(6));
    bool flag = false;
    bool f1 = false,f2 = false;
    for(int year = y;year <= 99991231;year++){
        for(int month = 1;month <= 12;month++){
            for(int day = 1; day <= 31;day++){
                if(month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12);
                else if(month == 2){
                    if((year % 4 == 0 && year % 100 != 0) || year % 400 == 0){if(day > 29) break;}
                    else {if(day > 28) break;}
                }else {
                    if(day > 30) break;
                }


                if(flag){
                    char tmp[10] = {0};
                    sprintf(tmp,"%d%02d%02d",year,month,day);
                    if(!f1 && check1(tmp)) {cout << tmp<<endl; f1 = true;}
                    if(!f2 && check2(tmp)) {cout << tmp; f2 = true;}
                    if(f1 && f2) return 0;
                }
                if(month == m && day == d) flag = true;
            }
        }
    }
    return 0;
}
```

## 例题3

2000年1月1日到2000年5月4日中间有多少天？

```c++
#include <bits/stdc++.h>
using namespace std;
int main(){
    int ans = 0;
    for(int year = 2000 ; year < 2001 ; year++){
        for(int month = 1 ; month <= 5; month++){
            for(int day = 1 ; day <= 31;day++){
                if(month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12) ; else if(month == 2){
                    if((year % 4 == 0 && year % 100 != 0) || year % 400 == 0){
                        if(day > 29) break;
                    }else{
                        if(day > 28) break;
                    }
                }else{
                    if(day > 30) break;
                }
                ans++;
                if(month == 5 && day == 4) {cout << ans;return 0;}
            }
        }
    }
}
```

125



1921年7月23日中午12时到2020年7月1日中午12时有多少分钟？

```c++
#include <bits/stdc++.h>
using namespace std;
int main(){
    int ans = 0,flag = 0;
    for(int year = 1921;year <= 2020; year++){
        for(int month = 1; month <= 12 ; month ++){
            for(int day = 1; day <= 31 ; day++){
   if(month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12); 
                else if(month == 2){
                    if( (year % 4 == 0 && year % 100 != 0) || year % 400 == 0){
                        if(day > 29) break;
                    }else{
                        if(day > 28) break;
                    }
            }else {
                    if(day > 30) break;
                }
                
                if(year == 1921 && month == 7 && day == 23) flag = 1;
                if(year == 2020 && month == 7 && day == 1) {cout << ans * 14400;return 0;}
                if(flag == 1) ans++;
            } 
        }
    }
    return 0;
}
```

注意一下这个某天的某个时间到另一天的这个时间应该算完了减少一天





对于周期问题可以用二项展开式

eg 今天是周一，20^21^天之后是周几 

20 = （14 + 6）^21^ = C~21~ ^0^ 14^0^ * 6 ^21^ + ...

= (216)^7^ = (210 + 6)^7^ = 6 ^7^ =216 * 216 * 6 = (210 + 6) * 216 * 6 = 6 * 216 * 6 = 216 % 7 = 6 

所以是周天
