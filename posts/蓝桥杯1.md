# 特殊整数对的数量

要求a和b满足

1. 1 $\le$ a < b $\le$ 10^6^
2. ab最大公约数1
3. a+b是2025的倍数

注意要用2025的倍数作为循环的条件减少循环的数量

```c++
  #include <iostream>
  using namespace std;
  int upperBound = 1000000;
  int a = 1,b = 1;
  int gcd(int x,int y){
    int temp;
    while(x != 0){
      temp = x;
      x = y % x;
      y = temp;
    }
    return y;
  }
  int main()
  {
    // 请在此输入您的代码
    // int num = 0;
    // for(;a < upperBound ; a++){
    //   for(b = 2025 - a;  b < upperBound; b+= 2025){
    //     if(b < a) continue;
    //     if(gcd(a,b) == 1) num++;
    //   }
    // }
    // cout << num;
    cout << 93816892;
    return 0;
  }
```

