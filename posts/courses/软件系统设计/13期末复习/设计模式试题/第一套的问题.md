1. 第一套的类图绘制箭头是不是反了，应该是subject里面加减observer嘛，不应该是subject指向observer嘛
2. 还有类图里面一般都是抽象用抽象 具体用具体么 （这个用就是指实线箭头）
3. 关于自己写代码的时候很多时候分不清变量怎么写
   1. 第一套第二大题什么系统的配置信息这种要不要当作属性写到类里面去
   2. 第一套的最后一道题的那个CPU GPU RAM到底写成类还是字符串这怎么判断啊
4. 如果让写抽象接口就必须写interface么，写abstract class行不行，这种有没有老师会要求？这俩区别是啥，就是interface方法默认是public abstract了，加上可以实现多个接口但是类只能继承一个，别的没啥区别了吧
5. 迭代器模式没学过
6. 第一套最后一道题：创造者模式不是可以写一个buildPart()方法么，里面的方法必须每个单独写一个体现出来么 ，我写一个buildPart方法里面一个个把每个部件都构建好可以么就是相当于director和builder融合到一起的那种