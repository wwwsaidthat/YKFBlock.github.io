# 树状数组

1. lowbit操作

   ```c++
   int lowbit(int x){
       return x & (-x);
   }
   ```

2. add操作

   ```c++
   void add(int pos,int k){
       for(int i = pos;i<=n;i+=lowbit(i)){
           tr[i] += k;
       }
   }
   ```

3. sum操作

   ```c++
   int sum(int pos){
       int sum = 0;
       for(int i = pos; i>= 0 ; i-=lowbit(i)){
           sum += tr[i];
       }
       return sum;
   }
   ```

4. 创建数组就把数组每个值对应用add操作就行了

5. https://www.bilibili.com/video/BV1pE41197Qj?t=117.9 实现就是O(log（n）)的然而整个操作也是O（logn）

6. 总体创建的时候的操作是O(NlogN)的

问题描述
小蓝有一个序列 a[1],a[2],...,a[n]，每次可以交换相邻的两个元素，代价为两个元素中较大的那个。

请问，要通过交换将序列变为从小到大递增的序列，总代价最少为多少？

输入格式
输入一行包含一个整数 n ，表示序列长度。

第二行包含 n 个整数，表示给定的序列。

输出格式
输出一行包含一个整数，表示最少代价的值。

```c++
#include "bits/stdc++.h"
using namespace std;
using ll = long long;
const int N = 1e6;
ll ans = 0;
int a[N + 1];
ll tr[N + 1];//索引是a[i] tr[i]代表这个数的出现的次数
int q[N + 1];
int rk[N + 1];
int lowbits(int x){return x & (-x);} 
void update(int pos,ll k){
	if(pos == 0) return;
	for(;pos <= N;pos += lowbits(pos))
	tr[pos] += k;
} 
ll query(int pos){
	if(pos == 0) return 0;
	ll sum = 0;
	for(;pos > 0;pos-=lowbits(pos)){
		sum += tr[pos];
	}
	return sum;
}

int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
	int n; cin>>n;
	for(int i = 1 ; i <= n ; i++) cin>>a[i];
	for(int i = n ; i >= 1 ; i--) {
		q[i] = query(a[i] - 1);
		update(a[i],1);
	}
	for(int i = 1; i <= n ; i++) ans+=1LL * q[i] * a[i];
	cout << ans;
	return 0;
}
```

# 单调栈

用途：找到比某个元素左/右边，大/小的最近的元素

拿左边小的最近的元素来说

栈里面可以先加入一个0，遍历数组中的元素，如果当前元素大于栈顶元素，那么栈顶元素就是这个数的对应的值，如果当前元素小于等于栈顶元素，把这个里面的元素一直pop，因为事先加入了一个0，我们肯定会找到一个比他小的元素，**栈里面加入的可以为元素的下标**，确定结果之后把这个元素加入到栈里面，就是思路