### 问题描述

给定一个长度为 n的序列 Ai，求 L,R 使 (R−L+1)⋅min⁡(AL,AL+1,…,AR)尽可能大，其中 min⁡ 表示最小值。

你只需要输出最大的值即可，不需要输出具体的 L,R。

### 输入格式

输入的第一行包含一个整数 n。

第二行包含 n 个整数，分别表示 A1,A2,…,An，相邻两个整数之间使用一个空格分隔。

### 输出格式

输出一行包含一个整数表示答案。

```c++
#include "bits/stdc++.h"
using namespace std;
using ll = long long;
const int N = 3e5 + 5;
stack<ll> skl,skr;
ll n,arr[N],l[N],r[N];
void findLeft(){
	skl.push(0);
	for(int i = 1 ; i <= n ; i++){
		while(arr[skl.top()] >= arr[i]) skl.pop();
		l[i] = skl.top();
		skl.push(i);
	} 
}
void findRight(){
	skr.push(n+1);
	for(int i = n ; i >= 1 ; i--){
		while(arr[skr.top()] >= arr[i]) skr.pop();
		r[i] = skr.top();
		skr.push(i);
	} 
}
int main(){
	cin >> n;
	for(int i = 1 ; i <= n;i++) cin >> arr[i];
	arr[0] = arr[n+1] = 0;
	findLeft();
	findRight();
	ll mx = 0;
	for(int i = 1 ; i <= n ; i++){
		mx = max(mx,(r[i] - l[i] - 1) *  arr[i]);
	}
	cout << mx;
	return 0;
}
```

单调栈：找到左右更大最近或者更小最近的元素O(N)的方法

如果找更大，就把栈顶更小的元素pop出去，记住先记录再push

1. **pop**
2. **记录**
3. **push**